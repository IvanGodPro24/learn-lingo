export type Items = {
  value: string;
  label: string;
};

export type MacImage = {
  [key: string]: string;
};

export type RatingOptions = {
  value: number;
  label: string;
};

export type PriceOptions = {
  value: [number, number];
  label: string;
};
